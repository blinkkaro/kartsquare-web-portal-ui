import { pageTab } from "@/lib/seo/buildMetadata";
import ListOfServices from "../../../components/pages/servicesList";

export const metadata = pageTab(
  "Browse services",
  "Discover and book home and professional services available on KartSquare.",
);

export default ListOfServices;
