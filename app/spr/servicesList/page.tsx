import { pageTab } from "@/lib/seo/buildMetadata";
import ProviderServicesList from "../../../components/pages/provider/servicesList";

export const metadata = pageTab(
  "My services",
  "Create, edit, and publish the services you offer on kartsquare.",
);

export default ProviderServicesList;
